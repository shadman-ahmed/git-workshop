# git paranoia

[Description]
git diff --staged
git reset --hard origin/branch

[Instruction]
[Example #1] - git diff --staged
1. touch file | echo 'Hello Inflack' > file
2. git add file
3. git diff --staged    # paranoia check

[Example #2] - bkash-500
1. git fetch origin
2. git checkout master
3. git reset --hard origin/master
4. git clean -d --force
